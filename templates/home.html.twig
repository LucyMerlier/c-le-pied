{% extends 'base.html.twig' %}

{% block title %}Accueil - {{ parent() }}{% endblock %}

{% block body %}

    <h1>C'est le pied !</h1>
    <p>Les petons de vos ami(e)s, des pattes de chatons trop mimis, ou tout simplement des pieds de lits, vous trouverez tous les pieds sont ici !</p>

    {% for picture in pictures %}
        <div class="picture-block">
            <h2><a href="{{ path('app_profile', { username: picture.owner.username }) }}">{{ picture.owner.username }}</a></h2>
            <a href="{{ path('app_show_picture', { id: picture.id }) }}">
                    <figure>
                        <img src="/assets/uploads/{{ picture.imageURL }}" alt="{{ picture.description }}">
                    </figure>
            </a>
            <p>{{ picture.description }}</p>
        </div>
    {% endfor %}

{% endblock %}
